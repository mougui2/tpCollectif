@page
@model WebAppliRazor.Secteur_DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Article</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Secteur.Nom)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Secteur.Nom)
        </dd>
        @foreach (var etagere in Model.Secteur.ListEtagere)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(ModelEtagere => ModelEtagere.Secteur.ListEtagere.Where( e => e == etagere))
            </dt>

            @*<dd class="col-sm-10">
                @Html.DisplayFor(ModelEtagere => etagere.Id)
            </dd>*@
        }

    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Secteur.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
