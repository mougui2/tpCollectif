@page
@model WebAppliRazor.Secteur_EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Article</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Secteur.Id" />
            <div class="form-group">
                <label asp-for="Secteur.Nom" class="control-label"></label>
                <input asp-for="Secteur.Nom" class="form-control" />
                <span asp-validation-for="Secteur.Nom" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                    <label asp-for="Secteur.ListEtagere" class="control-label"></label>
                    <input asp-for="Secteur.ListEtagere" class="form-control" />
                    <span asp-validation-for="Secteur.ListEtagere" class="text-danger"></span>
                </div>*@
            @foreach (var etagere in Model.Secteur.ListEtagere)
            {
                <div class="col-sm-2">
                    @Html.DisplayNameFor(ModelEtagere => ModelEtagere.Secteur.ListEtagere.Where(e => e == etagere))
                </div>

                @*<dd class="col-sm-10">
                        @Html.DisplayFor(ModelEtagere => etagere.Id)
                    </dd>*@
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
